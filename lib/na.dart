import 'package:flutter/material.dart';

class Na extends StatefulWidget {
  const Na({super.key});

  @override
  State<Na> createState() => _NaState();
}

class _NaState extends State<Na> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Text('dfdfdf'),
    );
  }
}
